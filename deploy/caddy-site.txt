# Paste this block into /root/familyoffice/deploy/Caddyfile on the VPS,
# alongside the office.gbxps.com and play.gbxps.com blocks.
# The app has its own login now, so no basic_auth is needed.

crm.gbxps.com {
	reverse_proxy crm:3000

	# Large SharePoint uploads go through the API.
	request_body {
		max_size 100MB
	}

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "no-referrer"
	}
}
